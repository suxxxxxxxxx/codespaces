<template>
    <div class="body">
      <ul>
        <li v-for="(item,index) in lists" :key="index">{{item}}</li>
      </ul>
      <button>修改</button>
    </div>
</template>

<script>
import{mapState} from 'vuex'
export default {
    computed:{
        ...mapState(['lists'])
    }
}
</script>

<style lang="css" scoped>

</style>